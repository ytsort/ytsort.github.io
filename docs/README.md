# docs

Everything this project has written down, apart from the handful of files a tool looks for at the
repo root (`README.md`, `CHANGELOG.md`, `LICENSE`, `CLAUDE.md`, `AGENTS.md`).

Open work lives in [`todo/`](todo). Odin reads both, so a project's documents and its outstanding
to-dos show up on the board next to its code.
